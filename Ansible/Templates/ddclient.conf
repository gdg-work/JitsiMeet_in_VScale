# Configuration file for ddclient generated by debconf
#
# /etc/ddclient.conf

protocol=dyndns2
use=if, if=eth0
server=api.dynu.com
login={{dyndns_email}}
password={{dyndns_password}}
{{dyndns_host}}.{{dyndns_domain}}
